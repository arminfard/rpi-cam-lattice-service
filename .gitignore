# Secrets / local config
.env
srt_target.env
# Daemon state (persisted ingress record; STATE_FILE in .env)
state.json

# MediaMTX (downloaded binary + logs; run scripts/install-mediamtx.sh)
mediamtx
mediamtx.log

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
