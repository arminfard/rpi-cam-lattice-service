"""rpi-cam-lattice-service — a Raspberry Pi camera Lattice integration built on the
Lattice SDK for gRPC (Connect) in Python.

This is a faithful port of the Go ``lattice-neuron`` scaffold: a long-running
daemon that publishes a simulated UAV asset entity to Lattice once per second
via the EntityManager ``PublishEntity`` RPC (Connect protocol).
"""

__version__ = "0.1.0"
